import { Text } from 'react-native';

import Screen from '@/components/Screen';

const SettingsView = () => (
  <Screen className="items-center justify-center">
    <Text className="text-center">Settings View</Text>
  </Screen>
);

export default SettingsView;
